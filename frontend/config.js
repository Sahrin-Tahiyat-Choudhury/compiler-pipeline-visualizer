// Single place to point the frontend at your deployed Lambda.
// Update API_URL after the Lambda Function URL step (not built yet).
// Left as a placeholder for now — does not affect anything we've
// built so far.
const CONFIG = {
  API_URL: "https://rnyotibd7usxxfaxifv2r746ri0gcucr.lambda-url.ap-south-1.on.aws/"
};